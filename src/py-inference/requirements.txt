# Core dependencies
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pyyaml>=6.0
httpx>=0.27.0

# GPU dependencies (install only on GPU machines)
vllm>=0.6.0
torch>=2.1.0

# Dev dependencies
pytest>=7.0
pytest-asyncio>=0.23.0
